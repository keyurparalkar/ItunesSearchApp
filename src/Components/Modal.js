import React from "react";
import {StyledModal} from "./StyledComponents.js";

export const Modal = (props) => {
    return(
        <StyledModal className="modal">
            <h1>Hello this is test modal!</h1>
        </StyledModal>
    );
}